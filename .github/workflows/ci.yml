name: CI

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  verify:
    name: verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify repository bootstrap
        run: |
          echo "Qarevo Health: CI is running."
          ls -la
